<!DOCTYPE html>
<html>
  <head>
    <title>Social Wall</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" type="text/css" href="normalize.css">
    <link rel="stylesheet" type="text/css" href="userpage.css">
  </head>
  <body>
    <div id="page">
      <form id="message-form" method="POST" action="/redirectHome">
        <button type="submit">To community wall</button>
      </form>
      <h1><%= client.blog_info(blog.to_s)["blog"]["name"] %></h1>
      <!--
      <form id="message-form" method="POST" action=<#%= "/subscribe/" + loggedUser.id.to_s + "/" + user.id.to_s %> >
        <button type="submit">Subscribe to this user</button>
      </form>
      -->
      <% (client.posts blog.to_s, :type => 'text', :limit => 5, :filter => 'text').each do |tumblr_post| %>
        <p><%= tumblr_post %></p>
      <% end %>
    </div>
  </body>
</html>